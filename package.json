{
  "name": "shopless",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development lerna exec --parallel npm run dev",
    "build": "lerna exec --parallel npm run build",
    "types": "typesync ./package.json && yarn"
  },
  "workspaces": [
    "./*"
  ],
  "devDependencies": {
    "lerna": "^3.0.0-rc.0",
    "typesync": "^0.5.1"
  }
}
